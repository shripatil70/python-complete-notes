# Python Lists

numbers = [1,2,3,4,5]

print(numbers)

# Access element
print(numbers[0])

# Add element
numbers.append(6)

# Remove element
numbers.remove(3)

# Length
print(len(numbers))

# Loop list
for num in numbers:
    print(num)
